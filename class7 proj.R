library(tidyverse)

life expec< read_csv("C:/User/sri ram/Desktop/R Programming/Datasets/lifeexp.csv")

life expec colnames(life expec)

life expec%>% filter(Year 2000)

life expec<- life expec %>% filter(Race == "All Races", Gender == "Both")

life expec%>%
  
  ggplot()

life expec %>%
  
  ggplot(aes(x = Year, y = Avg_Life_Expec)) life expec%>% y = Avg_Life_Expec)) +
  
  geom_line()

layer

ggplot(aes(x = Year. life expec 0/0 > 0/t # data layer ggplot(aes(x Year, y = Avg_Life_Expec)) + # axes
           
           geom_line()+#geom layer
             
             labs( # annotations layer title="United States Life Expectancy: 100 Years of
               
               Change', y = "Average Life Expectancy (Years)
               v <- c (3,1,TRUE,2+3i)
               t <- c (4,1,FALSE,2+3i)
               print(v & t)
               
               
               